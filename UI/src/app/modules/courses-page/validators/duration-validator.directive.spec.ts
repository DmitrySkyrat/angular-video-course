import { DurationDirective } from './duration-validator.directive';

describe('DurationDirective', (): void => {
  it('should create an instance', (): void => {
    const directive = new DurationDirective();
    expect(directive).toBeTruthy();
  });
});
